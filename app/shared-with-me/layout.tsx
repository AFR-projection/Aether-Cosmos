// The sidebar links here, so this route needs the same shell as /files and
// /shares — without it the page rendered chrome-less and had to hand-roll its
// own "back to my files" link.
export { default } from "@/components/layout/protected-layout";
